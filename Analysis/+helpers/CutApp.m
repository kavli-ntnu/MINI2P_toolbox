classdef (Sealed) CutApp
    % CutApp - Class with enumeration of supported clustering applications
    %
    properties (Constant)
        % all lower case, number is arbitary
        none = -1
        tint = 0
        mclust = 1
    end
end
